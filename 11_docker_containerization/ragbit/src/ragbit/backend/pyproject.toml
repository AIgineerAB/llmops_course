[project]
name = "backend"
version = "0.1.0"
description = "backend of ragbit project"
requires-python = ">=3.13"
dependencies = [
    "lancedb",
    "pydantic-ai",
    "fastapi",
    "uvicorn",
    "google-genai",
    "google-generativeai",
]
